<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta http-equiv="X-UA-Compatible" content="IE=edge" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>Lora Connect</title>
		<link
			rel="stylesheet"
			href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.1/dist/css/bootstrap.min.css"
			integrity="sha384-zCbKRCUGaJDkqS1kPbPd7TveP5iyJE0EjAuZQTgFLD2ylzuqKfdKlfG/eSrtxUkn"
			crossorigin="anonymous"
		/>
	</head>
	<body>
		<nav class="navbar sticky-top navbar-expand-lg navbar-dark bg-dark">
			<a class="navbar-brand" href="#">Lora Connect</a>
			<button
				class="navbar-toggler"
				type="button"
				data-toggle="collapse"
				data-target="#navbarSupportedContent"
				aria-controls="navbarSupportedContent"
				aria-expanded="false"
				aria-label="Toggle navigation"
			>
				<span class="navbar-toggler-icon"></span>
			</button>

			<div class="collapse navbar-collapse" id="navbarSupportedContent">
				<ul class="navbar-nav mr-auto">
					<li class="nav-item active">
						<a class="nav-link" href="/">Home</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="/map">Map</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="/history">History</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="/gateway/new">Submit gateway</a>
					</li>
				</ul>
				<div class="form-inline my-2 my-lg-0">
					<% if(!currentUser) { %>
					<a class="btn btn-outline-light my-2 mr-1 my-sm-0" href="/login">Login</a>
					<a class="btn btn-outline-light my-2 mr-1 my-sm-0" href="/register">Register</a>
					<% } else { %>
					<a class="btn btn-outline-light my-2 mr-1 my-sm-0" href="/users/<%= currentUser.username %>">
						<%= currentUser.fullname %>
					</a>
					<a class="btn btn-outline-light my-2 mr-1 my-sm-0" href="/logout">Logout</a>
					<% } %>
				</div>
			</div>
		</nav>
        <% if(error && error.length> 0) { %>
        <div class="alert alert-danger text-center mb-0" role="alert">
            <%= error %>
        </div>
        <% } if(success && success.length> 0) { %>
        <div class="alert alert-success text-center mb-0" role="alert">
            <%= success %>
        </div>
        <% } if(primary && primary.length> 0) { %>
        <div class="alert alert-primary text-center mb-0" role="alert">
            <%= primary %>
        </div>
        <% } %>